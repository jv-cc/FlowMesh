server:
  port: 19040

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:19040/eureka/
  instance:
    hostname: localhost